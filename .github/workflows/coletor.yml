name: coletor

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:
    inputs:
      forcar:
        description: 'Coletar os produtos agora, sem esperar a janela de 30 min'
        type: boolean
        default: true

concurrency:
  group: coletor
  cancel-in-progress: false

jobs:
  ciclo:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - run: pip install -r requirements.txt

      # A raspagem de catálogo NÃO roda mais aqui: virou o workflow
      # `catalogo.yml`, disparado pelo n8n. Ver coletor/catalogo.py.
      - name: Executar ciclo de coleta
        env:
          FIREBASE_SA_BASE64:     ${{ secrets.FIREBASE_SA_BASE64 }}
          TELEGRAM_BOT_TOKEN:     ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID:       ${{ secrets.TELEGRAM_CHAT_ID }}
          # 2h. O cron continua de 15 em 15 min de propósito: o GitHub atrasa e
          # PULA execuções agendadas, então a granularidade fina é o que garante
          # que a janela de 2h seja cumprida com atraso pequeno. Quem decide se
          # coleta é `sistema/controle`, nunca o horário do cron.
          INTERVALO_COLETA_MINUTOS: '120'
          # Só o disparo manual força. No `schedule`, `inputs` é vazio e isto
          # vira string vazia — que `config._booleano` lê como false. A raspagem
          # de catálogo NÃO é forçada por isto: segue no intervalo de 24h.
          FORCAR_COLETA:          ${{ inputs.forcar }}
          # 'true' = avisa a cada ciclo enquanto o preço estiver na faixa, sem
          # regra dos 5% e sem cooldown. Com ciclo de 30 min são até 48
          # mensagens por dia POR PRODUTO em faixa. Trocar para 'false' devolve
          # o comportamento de um alerta por oferta.
          ALERTA_REPETE_NO_RANGE: 'true'
          LIMIAR_SANIDADE:        '0.70'
          TETO_CENTAVOS:          '100000000'
          MARGEM_MEDIA_PCT:       '10'
          USER_AGENT:             'MonitorPrecos/1.0 (uso pessoal)'
        run: python -m coletor.main
