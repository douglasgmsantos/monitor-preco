firebase-admin==6.5.*
httpx==0.27.*
selectolax==0.3.*
pytest==8.3.*
pytest-asyncio==0.24.*
respx==0.21.*
# Análise estática de nome indefinido. Não é enfeite: os testes do Firestore
# exigem o emulador (e Java), ficam pulados na maioria das máquinas, e sem eles
# uma chamada a função apagada só aparece em produção — foi o que aconteceu em
# 2026-08-14. Ver tests/test_sanidade_estatica.py.
pyflakes==3.4.*
# Só os testes usam: é o que permite amarrar o JSON do n8n ao YAML do Actions
# (o `default: true` do input `forcar`). Sem ele esse teste vira skip.
pyyaml==6.*
